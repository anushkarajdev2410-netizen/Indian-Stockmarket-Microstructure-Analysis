Requirements · TXT
# ============================================================
# requirements.txt
# Indian Stock Market Microstructure Analysis — Capstone Project
# ============================================================
# Install everything with:
#     pip install -r requirements.txt
#
# Recommended: install inside a virtual environment first
#     python -m venv venv
#     source venv/bin/activate        (Windows: venv\Scripts\activate)
#     pip install -r requirements.txt
# ============================================================
 
# ---- Data acquisition ----
yfinance>=0.2.31          # Downloads historical OHLCV data from Yahoo Finance (NIFTY 50, BANK NIFTY, NSE stocks)
 
# ---- Core data manipulation ----
pandas>=2.0                # DataFrames: loading, cleaning, feature engineering, groupby/aggregation
numpy>=1.24                # Numerical operations (log returns, array math) underlying pandas/scikit-learn
 
# ---- Visualization ----
matplotlib>=3.7            # Base plotting library — time series, histograms, boxplots, scatter plots
seaborn>=0.12               # Statistical plotting built on matplotlib — heatmaps, distribution plots, styling
plotly>=5.15                # Optional interactive charts (zoom/hover) for exploratory work
 
# ---- Statistics ----
scipy>=1.10                 # Hypothesis testing: ttest_ind, f_oneway (ANOVA), pearsonr
 
# ---- Machine Learning ----
scikit-learn>=1.3           # StandardScaler, KMeans, AgglomerativeClustering, silhouette_score,
                             # LogisticRegression, DecisionTreeClassifier, train_test_split,
                             # confusion_matrix, classification_report
 
# ---- Notebook environment ----
jupyter>=1.0                 # Jupyter meta-package (kernel, ipython, etc.)
notebook>=7.0                 # Classic/modern Jupyter Notebook interface
 
# ============================================================
# Optional / nice-to-have (uncomment if needed)
# ============================================================
# nbformat>=5.9              # Only needed if programmatically building/editing .ipynb files
# ipykernel>=6.25             # Only needed if registering a custom named kernel
# openpyxl>=3.1               # Only needed if exporting processed data to .xlsx
 